## 원문 (English)

>> Will Sentance: I want to add some bonus stuff here, some extension with the latest JavaScript features.We're not going to map them out in quite the same amount of detail, but I think they're exciting andfun to see. So our copy array manipulate created a brand new array where the changes on each elementwere made, not changing the original array.The built-in map we just saw it.Also returns out brand new array, does not change the 123.If we'd instead declared my array.If we'd instead declared my array is 123and done my array.map, itwould have returned out a brand new array and now my array would still be the same.We could have assigned my array.map, the call to it,the output to same result, same result would be 246,but my array would still be 123.That is vital because I do not want my functions to have consequences outside of their explicitoutput. And if I'm changing the input data, that's a site that's going off over here.The output is not defined explicitly within the code's definition.Imagine if some of the key array mutate well,the key array methods in JavaScript around since day one did actually changethe input array. I mean that would be, and imagine if you do a whole wave of changes in 2015ES6, and you said, whatdoesn't matter? It's our mutating array methods, we're going to leave them as long as we needWell, that would be whathappened. Some of the built-in major methods in JavaScript actuallydo the work on the array directly.The one that they're being run on. Yep, key ones reverse sort and splice,but don't panic. Even though it's a crime.Do not panic. Fortunately we now have some new array methods built in that do not mutate,do not change their input array. I'll keep it fairly simple here,but I will show our memory.So in our memory we're declaring, you know, array 1 is our 123,we have array 2. Which is also 123,perhaps unhelpfully, maybe helpfully, I'm not sure. Now, the built-in methods here allow us totake our array 1, which we know is 123,and using this dot notation, we'll see all of this later on,run some built-in code. We've got parens on the end to show we're running some code,and that is going to head off to some built-in functions available to us in JavaScript.We'll see more of them in the REST of the session, that we can use on this 123 here.We find out about these functions in, as we heard from Tenzin, in the documentation,or in ChatGPT and so we have reverse,splice and sort. These are as old as time, they're built into JavaScript,their problem is that they change our input arraySo when we run arrayone.reverse and call it with parens, we're not getting a return value from this that we can assign toa new array with the array reversed, instead,we are going to mutate, change our inputarray, and that may not seem like the end of the world.But if we're wanting to, for example, take a list of tweets,songs or Spotify, whatever, and we know we're going to want to display them,but maybe we want to display them in reverse order, if we're running a reverse function on them in orderto then display them in reverse order, we definitely do not want to reverse the underlying data.Most all of the time, we do not want to reverse our original underlying data,because we're going to use that data when we are reversing it to be displayed or to be passed tosome other evaluation, not to actually change the intrinsic data.Especially in a language that's a lot of UI, you know, stuff that the users see,a lot of the time it's not trying to actually manipulate the data itself,but to take the data and manipulate it for display.So even more in UI oriented environments, you don't want to change underlying data,because you're constantly changing how the user sees it, but please don't change the underlying.So the fact that the built-in methods were nevertheless mutating,changing the underlying data is pretty odd.So our first call to reverse will change the input data andreplace it with 321. And next call to splice,and again I'm not here to sort of dig into the splice method design,but it takes a number of elements we want to change at a position we want to changethem, or takes a position we want to change them and the number we want to change,and then it gives us the number we want to change at that position,so our 321, taking in 116, isgoing to find position 1 in the array,so these two are fine, find position 1 in the array,change one element at that point, which is to be replaced with the number 6.OK, and then our sort isgoing to then order those automatically by our numerichierarchy. Do we like this?Everybody? A big no, exactly.So what are we grateful that JavaScript in under 30 years,no, yeah, under 30 years. This is a really fast update.They added non-mutating array methods, like the mapor copy array and manipulate that we just made.Inside of to reverse, to splice and to sorted, what is happening?The same has happened in our copy array and manipulate.We're creating a brand new array inside of it, into which we're putting the changed elements,the updated elements. OK, so let's do that fairly quickly,we have array 2 is 123We are then going tocall on array 2 the toreversed method, which is going to evaluate to a brandnew array 321.Well, we don't want to lose it, we're going to store it into a new label,reversed, 321. I then I'm going to call on the originalarray 2 again, our spliced, and it'sgoing to, oh geez, it's going to take our, hold on,I got my changes right here, yeah, it's going to take my position on the 1230,OK, this is hard to track.It's going to take out 123 and insert in the warmth position,the number 6, so we get 163 in to spliced.OK, so spliced, a brand new array with 163,and then finally, sorted is going to again take in our 123 and it's going to apply sort to itwell, it's already numerically sortedin ascending order, and so sorted is going to be123, so, hooray. Whereas our array 1 has been fully mutatedat this point to 136, our 123 is long gone for array one,our array 2 is still 123, our reversed has done the reversing in a new arrayour splice has done thesplicing in a new array, our sorted is done in a new array.And even better, we don't worry that we can't,we don't need to save all these labels separately, we could also know that the output according to reversedwill be 321, and then we could call on that to spliced.Skip the saving individual labels, we could call on that to sorted,and each time we could then run it on the mutated array, but it's a brand new mutated array,meaning our array 2 stays fixed.OK, in fact, probably I should have shown it that way, right, to show that we actually can still doall those changes mutating through, but our array 2 would have stayed fixed.OK, another interesting set of new functions, and again,we're not, I don't think I will diagram these out so much as to show you that these innovationskeep happening in JavaScript. This is a sort of fun extension.These innovations keep happening in JavaScript, this one from ES 2019,more 2023 and 2024. We have an ever moreuseful band of functions, and the idea with all of these is not to get hung up and say,oh my goodness, wow, the language is becoming harder to follow,but instead to go, great, I have some more built-in functions that I can go and read about in thedocs as I need them.So we have the ability to flatten, again, definitely non-mutating.I guarantee you if this had been created in the old days, it 100% would have been changing the originalarray, but here if we have an array containing another array inside of it,we can call the flatten, the flat method on it,and that will then take out the subarray and assign it as individual elementsin the main array, so our 12, subarray 122 becomes a single array,1212, 2. In this case we only had to go 1 layer deep,this flatten function, this flat function will actually allow us to go as many deep as we like,we can pass in infinity.Which is a terrible name forit, it defaults to one in this case, but we can pass in and go as deep as we wanttaking in multiple sub-arrays.Does the depth tell you how many layers you get out or how many you want to unpackexactly, yeah, yeah.You're going to get one layer out, that's the sort of flattening it is to get one layerout, that was added in 2019, 2023, we got,just my point again is to show we've got ongoing innovation in our array methods.We've got one here that allows us to find the last index, the last placing of a number in alist, in an array. Going backwards.So rather than fighting first index, in this case, we run a,in this case, I put an anonymous function here that takes in each element of the array going backwardsstarting with the last oneand runs a function onit. In this case, it's taking the element and then assess,is it equal to 2. If it is, you're going to return true.If it's not, you're going to return false.In the very first case of our find last index,it hits a 2, it returns true, and it's going to return out index 4 in our flattened array12122, because the first 2that it comes across is at position 4 in our 5 element array,01234, there you go. OK, and our last one here that's just been released and is actually a very popularfunction in SQL. Again, it's just to say that JavaScript continues to add,and, hard parts is never about laundry lists of features,so this is purely a little extension. Hard Parts is about a mental model,but it's just to see that JavaScript continues to addhigher-order functions, ones that we can pass in another function to,that do not change the input array, and here's another one.This is one that will allow us to take a list of values.In this case, we took in our flattened12122, right, the result of running the flat on the deep array, so12122. And we can evaluateeach element by passing in this odd or even function.And the two labels, or actually as many labels as we return,it's quite a sophisticated function under the hood, this group by,as many labels as we return will be the labels in an object that gets returned fromour group by function. So here, we had just even and odd,if we were to have a 3 label output, if this,then this label, if this, then this label, else,if this, then this label.We would have had 3 labels, and each of them, the,in this case, value that was associated with even,will go into the even array, the value that was associated with odd will go into the odd array.And folk, again, this is not a chance to map through the group by function so much as tosay our higher-order functions are everywhere.They make up a major part of JavaScript's array methods,and they make for readable code, look at how nice that is,group by this array, and then this condition,or this function that's running a conditional.And they do not, at least now, mutate,change the input array. Just as our copy array and manipulate did not either.It created a brand new array, it took in some instructions that were going to then be applied to eachelement of that array, returned out that new array with all of those changed elementsand that is our standardmodel in a vast amount of JavaScript array methods.

---

## 번역 (Korean)

>> Will Sentance: 여기서 보너스로 몇 가지, 최신 JavaScript 기능들에 대한 확장 내용을 추가하고자 합니다. 앞서와 똑같은 수준의 상세함으로 다루지는 않겠지만, 흥미롭고 재미있게 볼 만한 내용이라고 생각합니다. 우리가 만든 copyArrayAndManipulate는 각 요소에 변경이 적용된 완전히 새로운 배열을 만들어냈고, 원본 배열은 변경하지 않았습니다. 방금 본 내장 map도 마찬가지입니다. 이것 역시 완전히 새로운 배열을 반환하며, 123은 변경하지 않습니다.

만약 우리가 대신 myArray를 선언했다면, 즉 myArray를 123으로 선언하고 myArray.map을 실행했다면, 완전히 새로운 배열을 반환했을 것이고 myArray는 여전히 그대로였을 것입니다. myArray.map을 호출한 결과를 sameResult에 할당할 수도 있었을 것이고, sameResult는 246이 되었겠지만 myArray는 여전히 123이었을 것입니다.

이것은 매우 중요합니다. 저는 제 함수가 명시적인 출력(explicit output) 바깥에서 어떤 결과를 일으키기를 원하지 않기 때문입니다. 만약 입력 데이터를 변경한다면, 그것은 저 바깥 어딘가에서 벌어지는 부수 효과입니다. 그 출력은 코드의 정의 안에서 명시적으로 정의되어 있지 않습니다.

만약 JavaScript의 핵심 배열 메서드들 중 일부가 처음부터 실제로 입력 배열을 변경했다면 어떨지 상상해 보십시오. 그리고 2015년 ES6에서 대대적인 변화의 물결이 일었을 때, "뭐가 문제야? 그건 우리의 변경(mutating) 배열 메서드들이고, 필요한 만큼 그대로 두겠어"라고 말했다고 상상해 보십시오. 실제로 그런 일이 일어났습니다. JavaScript의 주요 내장 메서드들 중 일부는 실제로 배열에 직접 작업을 수행합니다. 즉, 그 메서드가 실행되는 대상 배열 자체를 말이죠. 그렇습니다, 대표적인 것이 reverse, sort, splice입니다. 하지만 당황하지 마십시오. 이것이 비록 죄악이긴 하지만, 당황하지 마십시오.

다행히도 이제 우리에게는 입력 배열을 변경(mutate)하지 않는 새로운 내장 배열 메서드들이 있습니다. 여기서는 꽤 간단하게 다루겠지만, 메모리는 보여드리겠습니다.

메모리에서 array1을 123으로 선언하고, array2도 있습니다. 이것 역시 123인데, 어쩌면 헷갈릴 수도 있고 어쩌면 도움이 될 수도 있겠습니다. 잘 모르겠네요. 자, 여기 있는 내장 메서드들은 우리가 123이라는 것을 아는 array1을 가져와서, 이 점 표기법(dot notation)을 사용해 — 이 부분은 나중에 모두 다룰 것입니다 — 어떤 내장 코드를 실행할 수 있게 해줍니다. 끝에 괄호가 붙어 있는 것은 우리가 어떤 코드를 실행한다는 것을 나타내며, 그것은 JavaScript에서 사용 가능한 어떤 내장 함수로 향하게 됩니다. 세션의 나머지 부분에서 이 123에 사용할 수 있는 더 많은 메서드들을 보게 될 것입니다.

Tenzin에게서 들었듯이, 이런 함수들에 대해서는 문서(documentation)나 ChatGPT에서 알아볼 수 있습니다. 그래서 우리에게는 reverse, splice, sort가 있습니다. 이것들은 아주 오래된 것들로 JavaScript에 내장되어 있는데, 문제는 이들이 우리의 입력 배열을 변경한다는 점입니다.

그래서 array1.reverse를 괄호와 함께 호출하면, 배열이 뒤집힌 새 배열을 할당할 수 있는 반환값을 얻는 것이 아니라, 대신 우리의 입력 배열을 변경(mutate)하게 됩니다. 그리고 그것이 세상이 끝날 일처럼 보이지는 않을 수도 있습니다.

하지만 예를 들어 트윗 목록이나 노래, Spotify 같은 것의 목록을 가져와서 그것들을 화면에 표시하려 한다고 해봅시다. 그런데 역순으로 표시하고 싶을 수도 있습니다. 역순으로 표시하기 위해 reverse 함수를 실행한다면, 우리는 결코 기반이 되는 원본 데이터(underlying data)를 뒤집고 싶지는 않을 것입니다. 거의 대부분의 경우, 우리는 원본 데이터 자체를 뒤집기를 원하지 않습니다. 왜냐하면 그 데이터를 뒤집는 것은 화면에 표시하기 위해서거나 다른 평가로 전달하기 위해서이지, 데이터의 본질 자체를 바꾸기 위한 것이 아니기 때문입니다.

특히 UI가 많은 언어, 그러니까 사용자가 보게 되는 것들을 다루는 환경에서는, 많은 경우 데이터 자체를 조작하려는 것이 아니라 데이터를 가져와 표시를 위해 조작하려는 것입니다. 그래서 UI 지향적인 환경에서는 더욱더 원본 데이터를 변경하고 싶지 않습니다. 사용자가 보는 방식은 끊임없이 바꾸게 되지만, 제발 그 기반 데이터는 바꾸지 마십시오.

따라서 그럼에도 불구하고 내장 메서드들이 원본 데이터를 변경(mutating)하고 있었다는 사실은 상당히 이상한 일입니다.

그래서 첫 번째 reverse 호출은 입력 데이터를 변경해서 321로 바꿉니다. 그리고 다음으로 splice 호출인데, 다시 말하지만 저는 여기서 splice 메서드의 설계를 파고들려는 것은 아닙니다. 이 메서드는 변경하고자 하는 위치와 변경하고자 하는 요소의 개수를 받고, 그 위치에서 변경할 값을 받습니다. 그래서 우리의 321에 (1, 1, 6)을 넣으면, 배열에서 인덱스 1을 찾아서 — 앞의 두 인자는 그것을 지정하는 것이고 — 그 지점에서 하나의 요소를 변경하는데, 그것은 숫자 6으로 대체됩니다. 좋습니다. 그리고 나서 sort는 숫자 순서 체계에 따라 자동으로 정렬하게 됩니다.

이게 마음에 드십니까? 여러분? 완전히 아니죠, 그렇습니다.

그래서 JavaScript가 30년도 채 안 되어서, 아니 네, 30년이 채 안 되어서 — 정말 빠른 업데이트죠 — 변경하지 않는(non-mutating) 배열 메서드들을 추가해준 것에 감사할 따름입니다. 우리가 방금 만든 map이나 copyArrayAndManipulate처럼 말이죠.

toReversed, toSpliced, toSorted 내부에서는 무슨 일이 일어나고 있을까요? 우리의 copyArrayAndManipulate에서 일어난 것과 똑같은 일이 일어납니다. 그 내부에서 완전히 새로운 배열을 만들고, 그 안에 변경된 요소들, 업데이트된 요소들을 넣는 것입니다.

좋습니다, 그럼 빠르게 해봅시다. array2는 123입니다. 그런 다음 array2에 toReversed 메서드를 호출하면, 완전히 새로운 배열 321로 평가됩니다. 그것을 잃어버리고 싶지 않으니 새로운 라벨 reversed에 저장하면, 321이 됩니다.

그다음 다시 원본 array2에 toSpliced를 호출합니다. 이건, 아 잠깐만요, 여기 제 변경 내용이 있네요. 네, 1230 위치를 받아서... 좋습니다, 이건 따라가기가 어렵네요. 123에서 해당 위치의 값을 빼내고 그 자리에 숫자 6을 넣습니다. 그래서 toSpliced에서 163을 얻게 됩니다. 좋습니다, 그래서 spliced는 163을 가진 완전히 새로운 배열입니다.

그리고 마지막으로, sorted는 다시 우리의 123을 받아서 정렬을 적용합니다. 그런데 이미 숫자 오름차순으로 정렬되어 있으므로 sorted는 123이 됩니다. 자, 만세입니다.

반면 array1은 이 시점에 완전히 변경(mutate)되어 136이 되었고, array1의 123은 이미 사라진 지 오래입니다. 우리의 array2는 여전히 123이고, reversed는 새 배열에서 뒤집기를 수행했으며, spliced는 새 배열에서 splice를 수행했고, sorted도 새 배열에서 수행되었습니다.

더 좋은 점은, 이 모든 라벨을 따로따로 저장할 필요가 없다는 것입니다. toReversed의 출력이 321이라는 것을 알기 때문에, 그 결과에 바로 toSpliced를 호출할 수 있습니다. 개별 라벨 저장을 건너뛰고, 그 결과에 다시 toSorted를 호출할 수 있습니다. 그리고 매번 변경된 배열에 대해 실행하되, 그것은 완전히 새로운 변경된 배열이므로 우리의 array2는 그대로 고정되어 있다는 뜻입니다. 사실 아마 그렇게 보여드렸어야 했을 것 같습니다. 그 모든 변경을 연쇄적으로 수행하면서도 array2는 그대로 유지된다는 것을 보여주기 위해서 말이죠.

좋습니다, 또 다른 흥미로운 새 함수들이 있습니다. 다시 말하지만, 이것들을 자세히 다이어그램으로 그리지는 않을 것 같고, 다만 JavaScript에서 이런 혁신이 계속 일어나고 있다는 것을 보여드리려는 것입니다. 이건 일종의 재미있는 확장입니다. 이런 혁신은 JavaScript에서 계속 일어나고 있는데, 이것은 ES 2019에서 나온 것이고, 더 많은 것들이 2023년과 2024년에 나왔습니다.

우리에게는 점점 더 유용한 함수들의 모음이 있습니다. 이 모든 것에 대한 태도는 "세상에, 이 언어가 점점 따라가기 어려워지는군"이라며 막막해하는 것이 아니라, "좋아, 필요할 때 문서에서 찾아 읽어볼 수 있는 내장 함수가 더 생겼구나"라고 받아들이는 것입니다.

그래서 우리에게는 평탄화(flatten) 기능이 있습니다. 다시 말하지만 이것은 확실히 변경하지 않습니다(non-mutating). 장담하건대 이것이 옛날에 만들어졌다면 100% 원본 배열을 변경했을 것입니다. 하지만 여기서는, 내부에 다른 배열을 포함하고 있는 배열이 있을 때 flat 메서드를 호출할 수 있고, 그러면 그 하위 배열을 꺼내어 메인 배열의 개별 요소들로 넣어줍니다. 그래서 [1, 2, [1, 2, 2]]가 하나의 배열 [1, 2, 1, 2, 2]가 됩니다.

이 경우에는 한 단계만 들어가면 됐습니다. 이 flat 함수는 실제로 우리가 원하는 만큼 깊이 들어갈 수 있게 해줍니다. Infinity를 전달할 수도 있는데, 이건 이름이 정말 형편없죠. 이 경우 기본값은 1이지만, 여러 하위 배열을 다룰 때 원하는 만큼 깊이 들어가도록 값을 전달할 수 있습니다.

(질문) depth는 몇 개의 층이 밖으로 나오는지를 말하는 건가요, 아니면 몇 개를 풀어내고 싶은지를 말하는 건가요? — 바로 그겁니다, 네, 네. 한 층이 밖으로 나오게 되는 것이고, 그것이 한 층을 꺼낸다는 의미의 평탄화입니다.

그것은 2019년에 추가되었고, 2023년에는 또 다른 것을 얻었습니다. 다시 한번 제 요점은 우리의 배열 메서드에 지속적인 혁신이 있다는 것을 보여주는 것입니다. 여기 마지막 인덱스, 즉 목록이나 배열에서 어떤 숫자가 마지막으로 위치한 곳을 찾을 수 있게 해주는 것이 있습니다. 뒤에서부터 거슬러 올라가면서 말이죠.

그래서 첫 번째 인덱스를 찾는 것과 달리, 이 경우에는 익명 함수(anonymous function)를 넣어서 마지막 요소부터 시작해 배열의 각 요소를 거꾸로 받아 함수를 실행합니다. 이 경우 요소를 받아서 그것이 2와 같은지 평가합니다. 같다면 true를 반환하고, 아니라면 false를 반환합니다.

findLastIndex의 아주 첫 번째 경우에 2를 만나서 true를 반환하고, 평탄화된 배열 [1, 2, 1, 2, 2]에서 인덱스 4를 반환하게 됩니다. 왜냐하면 처음 만나는 2가 5개 요소 배열에서 인덱스 4에 있기 때문입니다. 0, 1, 2, 3, 4, 바로 그렇습니다.

좋습니다, 그리고 여기 마지막 것은 막 출시된 것으로, 사실 SQL에서 매우 인기 있는 함수입니다. 다시 말하지만, 이것은 그저 JavaScript가 계속해서 기능을 추가하고 있다는 것을 말하려는 것입니다. Hard Parts는 결코 기능 목록을 나열하는 것에 관한 것이 아니므로, 이것은 순전히 작은 확장일 뿐입니다. Hard Parts는 정신 모델(mental model)에 관한 것이지만, 그저 JavaScript가 계속해서 고차 함수(higher-order function) — 다른 함수를 인자로 전달할 수 있고 입력 배열을 변경하지 않는 함수 — 를 추가하고 있다는 것을 보는 것입니다. 그리고 여기 또 하나가 있습니다.

이것은 값들의 목록을 받을 수 있게 해주는 함수입니다. 이 경우, 평탄화된 [1, 2, 1, 2, 2]를 받았습니다. 맞죠, 깊은 배열에 flat을 실행한 결과인 [1, 2, 1, 2, 2] 말입니다. 그리고 이 odd 또는 even 함수를 전달함으로써 각 요소를 평가할 수 있습니다.

그리고 두 개의 라벨, 사실은 우리가 반환하는 만큼의 라벨이 — 이 groupBy는 내부적으로 꽤 정교한 함수입니다 — 우리가 반환하는 만큼의 라벨이 groupBy 함수에서 반환되는 객체의 키가 됩니다. 여기서는 even과 odd만 있었지만, 만약 3개의 라벨 출력을 갖게 된다면, 즉 "이 조건이면 이 라벨, 이 조건이면 이 라벨, 그 외에는 이 라벨"이라고 했다면 3개의 라벨을 갖게 되었을 것입니다. 그리고 각각에 대해, 이 경우 even과 연관된 값은 even 배열로 들어가고, odd와 연관된 값은 odd 배열로 들어갑니다.

여러분, 다시 말하지만 이것은 groupBy 함수를 하나하나 짚어보려는 기회라기보다는, 고차 함수가 어디에나 있다는 것을 말하려는 것입니다. 고차 함수는 JavaScript 배열 메서드의 주요한 부분을 차지하며, 읽기 좋은 코드를 만들어줍니다. 얼마나 멋진지 보십시오. "이 배열을 그룹화하라, 그리고 이 조건으로" 또는 "조건을 실행하는 이 함수로"라고 읽히니까요.

그리고 이들은, 적어도 이제는, 입력 배열을 변경(mutate)하지 않습니다. 우리의 copyArrayAndManipulate가 그러지 않았던 것과 마찬가지로 말이죠. 그것은 완전히 새로운 배열을 만들었고, 그 배열의 각 요소에 적용될 지시사항을 받아서, 변경된 모든 요소들을 담은 새 배열을 반환했습니다. 그리고 그것이 방대한 양의 JavaScript 배열 메서드에서 우리의 표준 모델입니다.
